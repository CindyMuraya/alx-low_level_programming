#include <stdio.h>

/**
 * Description: main - prints single digit of base 10 from 0
 * Return: 0 if successful
 */
int main(void)
{
	int n;

	for (n = 0; number < 10; n++)
	{
		putchar("%d", n);
	}
	putchar('\n');
	return (0);
}
